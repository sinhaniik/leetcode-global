import React from 'react';

type Props = {};
const ProblemStatement = (props: Props) => {
    return (
        <div className="h-screen" >
            <h1>Problem Statement</h1>
        </div>
    );
};

export default ProblemStatement